language: java

jdk:
  - openjdk7
  - openjdk8
  - openjdk9 
  - oraclejdk10
  - oraclejdk8
 # - oraclejdk9
 
build:

  ci:
    - java -version
    - echo $SHIPPABLE_JDK_VERSION
    - mvn --version    
    - ant -version 
    - gradle --version
    - docker pull shippabledocker/sample_node_pvt:ecr
